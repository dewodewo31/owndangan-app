task: owndangan-feature-wave todo 33 — repo hygiene
files:
  backend/-l — deleted (untracked 0-byte stray; root -l was already absent)
  .gitignore — appended "-l" and "*.tsbuildinfo" under "# Stray artifacts"
  frontend/tsconfig.tsbuildinfo — git rm --cached (file kept locally, now ignored)
gates:
  git ls-files | grep tsbuildinfo -> 0 tracked
  git status --porcelain -> no "-l" artifacts; only staged D of tsbuildinfo from this change itself
notes:
  - No real source files touched; WIP modified files untouched.
