task: owndangan-feature-wave todo 22 — payment-success email on settlement
build_command: go build ./...
build_exit_code: 0
test_command: go test ./internal/api/handler/ -run TestPaymentWebhook -count=1 -v && go test ./internal/service/ -count=1
test_exit_code: 0 (handler 2 passed; service package ok)
tests:
  TestPaymentWebhook_Settlement_SendsEmailOnce — settlement webhook -> exactly 1 email to payer (recipient+subject "aktif"+amount "99.000" asserted); duplicate identical webhook -> still exactly 1 (idempotency guard)
  TestPaymentWebhook_Deny_NoEmail — deny webhook -> zero emails
files:
  backend/internal/service/subscription_service.go — ActivateOnSettlement now returns (*model.Subscription, error); single caller updated
  backend/internal/service/payment_service.go — EmailSender interface seam; emailSvc field + ctor param; sendPaymentSuccessEmail (best-effort lookups, renders template, SendAsync AFTER txn persisted + activation succeeded; never fails webhook); formatIDR helper
  backend/internal/api/server.go — Dependencies.EmailSender (defaults to real SMTP service from cfg.SMTP when nil); wired into NewPaymentService
  backend/internal/repository/transaction_repo.go — FIX pre-existing bug exposed by new tests: IsProcessed queried nonexistent column request_id; model key is transaction_id (matches MarkProcessed insert). No webhook tests existed before, so this was never caught.
  backend/internal/api/handler/payment_email_test.go (new) — recording EmailSender fake injected via testEmailSender hook set BEFORE setupAuthTestServer
  backend/internal/api/handler/auth_test.go — package-level testEmailSender injection point in deps
  backend/internal/service/payment_test.go — noopEmailSender for new ctor arg
notes:
  - Dedupe is double-layered: WebhookIdempotency IsProcessed early-return + status-transition guard (txn.Status=="settlement" && internal=="settlement").
  - Webhook response/idempotency behavior untouched; email fires after txnRepo.Update + ActivateOnSettlement succeed.
pre_existing_failures: make test-unit shows ONLY the 4 documented failures (TestAuth_Register_ValidationError, TestE2E_ValidationError, TestEditor_Template_AssignAndList, TestAPI_PackagesList). Not fixed.
