task: owndangan-feature-wave todo 30 — onboarding auto-login after registration
build_command: npm run build
build_exit_code: 0 (14/14 pages)
files:
  frontend/src/providers/auth-context.tsx — register(): guard added; if API response lacks access_token/refresh_token -> router.push("/login?notice=sesi") and return WITHOUT storing session. Happy path already stored tokens via existing mechanism (localStorage access_token/refresh_token/user + api.defaults header) and redirected to /dashboard — unchanged.
  frontend/src/app/login/page.tsx — notice banner: useEffect reads ?notice=sesi -> shows "Registrasi berhasil. Silakan masuk dengan akun Anda." above the form.
notes:
  - Auto-login itself already existed in the auth context (register consumed the backend token pair, stored via same mechanism as login, pushed /dashboard). The plan's remaining gap was the token-missing fallback, now implemented.
  - Plan referenced src/app/(auth)/register/page.tsx — actual path is src/app/register/page.tsx (no route group).
  - No new auth endpoint; no second login request; reuses existing storage helpers only.
