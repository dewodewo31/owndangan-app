task: owndangan-feature-wave todo 23 — daily subscription expiry reminder ticker
build_command: go build ./...
build_exit_code: 0
test_command: go test ./internal/api/handler/ -run TestExpiryWorker -count=1 -v
test_exit_code: 0 (2 passed)
tests:
  TestExpiryWorker_Reminder_Dedupe — sub expiring in exactly 7d -> one reminder email; second RunOnce same day -> still 1 (audit-log dedupe action=email.expiry_reminder/entity=subscription/day-start); status stays active
  TestExpiryWorker_Expired_FlipsStatus — expired active sub -> expired notice email + status flipped to "expired"; rerun does not re-notify
files:
  backend/internal/service/email/worker.go (new) — ExpiryWorker{subs,users,pkgs,audit,sender,renewURL,Clock}; RunOnce scans [now+6d,now+8d] reminders with per-day audit dedupe, then expired-active scan -> notify + status flip; Sender interface = SendWithRetry (*Service implements)
  backend/internal/repository/transaction_repo.go — SubscriptionRepository.ListExpiringBetween/ListExpiredActive; AuditLogRepository.ExistsSince
  backend/cmd/server/main.go — runExpiryReminderWorker goroutine: RunOnce at startup then 24h ticker; stopped via context cancel on SIGINT/SIGTERM; renewURL from APP_PUBLIC_URL env (fallback http://localhost:3000) + /dashboard/billing
  backend/internal/api/handler/expiry_worker_test.go (new) — real-DB harness, injected fixed clock
  backend/internal/api/handler/payment_email_test.go — recEmailSender also implements SendWithRetry
  backend/internal/service/mock_test.go — mock stubs for new interface methods (+time import)
notes:
  - No new table/migration (audit_log dedupe per plan); no queue dependency; ticker stoppable via ctx cancel.
  - Plan's "offset configurable" not implemented — acceptance criteria only require mocked-clock tests; interval is a constant 24h with startup run.
pre_existing_failures: make test-unit shows ONLY the 4 documented failures (TestAuth_Register_ValidationError, TestE2E_ValidationError, TestEditor_Template_AssignAndList, TestAPI_PackagesList). Not fixed.
